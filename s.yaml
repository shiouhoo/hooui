edition: 3.0.0 # 命令行YAML规范版本，遵循语义化版本（Semantic Versioning）规范
name: hooui # 项目名称

resources:
  hooui: #  项目名称
    component: fc3  # 组件名称
    props:
      region: cn-chengdu # 地域
      code: docs/.vitepress/dist
      handler: index.handler
      runtime: nodejs16
      funtionaName: vitepressFunc
      triggers:
        - triggerName: httpTrigger
          triggerType: http
          triggerConfig:
            authType: anonymous
            methods: 
              - GET